[
  { "id": "total-components", "question": "How many components are there in total?",
    "expected_sql": "select count(*) from components" },
  { "id": "count-by-status", "question": "How many components are in each status?",
    "expected_sql": "select current_status, count(*) from components group by current_status" },
  { "id": "flagged-count", "question": "How many components are currently flagged?",
    "expected_sql": "select count(*) from components where current_status = 'flagged'" },
  { "id": "completed-count", "question": "How many components have been completed?",
    "expected_sql": "select count(*) from components where current_status = 'completed'" },
  { "id": "pending-count", "question": "How many components are still pending?",
    "expected_sql": "select count(*) from components where current_status = 'pending'" },
  { "id": "in-progress-count", "question": "How many components are being worked on right now?",
    "expected_sql": "select count(*) from components where current_status = 'in_progress'" },
  { "id": "station-count", "question": "How many workstations are there?",
    "expected_sql": "select count(*) from workstations" },
  { "id": "active-station-count", "question": "How many workstations are active?",
    "expected_sql": "select count(*) from workstations where is_active = true" },

  { "id": "in-progress-names", "question": "Which components are currently in progress?",
    "expected_sql": "select name from components where current_status = 'in_progress'" },
  { "id": "flagged-names", "question": "List the names of the flagged components.",
    "expected_sql": "select name from components where current_status = 'flagged'" },
  { "id": "pending-names", "question": "Which components are still pending?",
    "expected_sql": "select name from components where current_status = 'pending'" },
  { "id": "completed-names", "question": "List the names of the completed components.",
    "expected_sql": "select name from components where current_status = 'completed'" },
  { "id": "all-component-names", "question": "List every component name.",
    "expected_sql": "select name from components limit 100" },

  { "id": "overdue-count", "question": "How many components are overdue?",
    "expected_sql": "select count(*) from components where deadline < now() and current_status <> 'completed'" },
  { "id": "overdue-names", "question": "Which components are overdue?",
    "expected_sql": "select name from components where deadline < now() and current_status <> 'completed'" },
  { "id": "no-deadline-count", "question": "How many components have no deadline set?",
    "expected_sql": "select count(*) from components where deadline is null" },
  { "id": "earliest-deadline-component", "question": "Which component has the earliest deadline?",
    "expected_sql": "select name from components where deadline is not null order by deadline asc limit 1" },
  { "id": "latest-deadline-component", "question": "Which component has the latest deadline?",
    "expected_sql": "select name from components where deadline is not null order by deadline desc limit 1" },
  { "id": "due-next-24h-count", "question": "How many components are due in the next 24 hours?",
    "expected_sql": "select count(*) from components where deadline between now() and now() + interval '24 hours'" },
  { "id": "overdue-flagged-count", "question": "How many overdue components are also flagged?",
    "expected_sql": "select count(*) from components where deadline < now() and current_status = 'flagged'" },
  { "id": "components-with-deadline-count", "question": "How many components have a deadline set?",
    "expected_sql": "select count(*) from components where deadline is not null" },

  { "id": "qa-station-names", "question": "Which workstations are QA stations?",
    "expected_sql": "select name from workstations where is_qa = true" },
  { "id": "final-station-name", "question": "Which workstation is the final station?",
    "expected_sql": "select name from workstations where is_final_station = true" },
  { "id": "qa-station-count", "question": "How many QA stations are there?",
    "expected_sql": "select count(*) from workstations where is_qa = true" },
  { "id": "inactive-station-count", "question": "How many workstations have been deactivated?",
    "expected_sql": "select count(*) from workstations where is_active = false" },
  { "id": "active-station-names", "question": "List the names of all active workstations.",
    "expected_sql": "select name from workstations where is_active = true" },
  { "id": "station-locations", "question": "What is the location of each workstation?",
    "expected_sql": "select name, location from workstations" },
  { "id": "stations-without-location-count", "question": "How many workstations have no location recorded?",
    "expected_sql": "select count(*) from workstations where location is null" },

  { "id": "total-log-count", "question": "How many status changes have been recorded in total?",
    "expected_sql": "select count(*) from status_logs" },
  { "id": "distinct-workers-count", "question": "How many different workers have recorded a scan?",
    "expected_sql": "select count(distinct worker_name) from status_logs where worker_name is not null" },
  { "id": "scans-per-worker", "question": "How many scans has each worker recorded?",
    "expected_sql": "select worker_name, count(*) from status_logs where worker_name is not null group by worker_name" },
  { "id": "busiest-worker", "question": "Which worker has recorded the most scans?",
    "expected_sql": "select worker_name from status_logs where worker_name is not null group by worker_name order by count(*) desc limit 1" },
  { "id": "flag-events-count", "question": "How many times has a component been flagged?",
    "expected_sql": "select count(*) from status_logs where to_status = 'flagged'" },
  { "id": "rework-events-count", "question": "How many times has a flagged component been sent back for rework?",
    "expected_sql": "select count(*) from status_logs where from_status = 'flagged' and to_status = 'in_progress'" },
  { "id": "completion-events-count", "question": "How many components have ever been marked completed?",
    "expected_sql": "select count(*) from status_logs where to_status = 'completed'" },
  { "id": "last-updated-by-names", "question": "Who last updated each component?",
    "expected_sql": "select name, last_updated_by from components" },

  { "id": "changes-per-station", "question": "How many status changes happened at each workstation?",
    "expected_sql": "select w.name, count(*) from status_logs s join workstations w on w.id = s.workstation_id group by w.name" },
  { "id": "busiest-station", "question": "Which workstation has the most status changes?",
    "expected_sql": "select w.name from status_logs s join workstations w on w.id = s.workstation_id group by w.name order by count(*) desc limit 1" },
  { "id": "distinct-components-per-station", "question": "How many distinct components has each workstation handled?",
    "expected_sql": "select w.name, count(distinct s.component_id) from status_logs s join workstations w on w.id = s.workstation_id group by w.name" },
  { "id": "most-handled-station", "question": "Which workstation has handled the most distinct components?",
    "expected_sql": "select w.name from status_logs s join workstations w on w.id = s.workstation_id group by w.name order by count(distinct s.component_id) desc limit 1" },
  { "id": "components-per-current-station", "question": "How many components are currently at each workstation?",
    "expected_sql": "select current_workstation_name, count(*) from components where current_workstation_id is not null group by current_workstation_name" },
  { "id": "quietest-station", "question": "Which workstation has the fewest status changes?",
    "expected_sql": "select w.name from status_logs s join workstations w on w.id = s.workstation_id group by w.name order by count(*) asc limit 1" },

  { "id": "changes-last-24h", "question": "How many status changes happened in the last 24 hours?",
    "expected_sql": "select count(*) from status_logs where timestamp >= now() - interval '24 hours'" },
  { "id": "completed-last-7d", "question": "How many components were completed in the last 7 days?",
    "expected_sql": "select count(*) from status_logs where to_status = 'completed' and timestamp >= now() - interval '7 days'" },
  { "id": "changes-per-day", "question": "How many status changes happened per day?",
    "expected_sql": "select date_trunc('day', timestamp) d, count(*) from status_logs group by d order by d" },
  { "id": "changes-today-count", "question": "How many status changes happened today?",
    "expected_sql": "select count(*) from status_logs where timestamp >= current_date" },
  { "id": "completed-today-count", "question": "How many components were completed today?",
    "expected_sql": "select count(*) from status_logs where to_status = 'completed' and timestamp >= current_date" },
  { "id": "flagged-today-count", "question": "How many components were flagged today?",
    "expected_sql": "select count(*) from status_logs where to_status = 'flagged' and timestamp >= current_date" },
  { "id": "changes-last-hour", "question": "How many status changes happened in the last hour?",
    "expected_sql": "select count(*) from status_logs where timestamp >= now() - interval '1 hour'" },
  { "id": "components-updated-today", "question": "How many components were updated today?",
    "expected_sql": "select count(*) from components where updated_at >= current_date" },

  { "id": "most-recently-updated-component", "question": "Which component was updated most recently?",
    "expected_sql": "select name from components where updated_at is not null order by updated_at desc limit 1" },
  { "id": "oldest-component", "question": "Which component was created first?",
    "expected_sql": "select name from components order by created_at asc limit 1" },
  { "id": "newest-component", "question": "Which component was created most recently?",
    "expected_sql": "select name from components order by created_at desc limit 1" },
  { "id": "latest-scan-component", "question": "Which component was scanned most recently?",
    "expected_sql": "select c.name from status_logs s join components c on c.id = s.component_id order by s.timestamp desc limit 1" },
  { "id": "most-scanned-component", "question": "Which component has been scanned the most times?",
    "expected_sql": "select c.name from status_logs s join components c on c.id = s.component_id group by c.name order by count(*) desc limit 1" },
  { "id": "top-3-scanned-components", "question": "What are the top 3 most scanned components?",
    "expected_sql": "select c.name from status_logs s join components c on c.id = s.component_id group by c.name order by count(*) desc limit 3" },

  { "id": "avg-changes-per-component", "question": "What is the average number of status changes per component?",
    "expected_sql": "select avg(c) from (select component_id, count(*) c from status_logs group by component_id) t" },
  { "id": "longest-idle-gap", "question": "What is the longest time a component waited between two status changes?",
    "expected_sql": "select max(gap) from (select timestamp - lag(timestamp) over (partition by component_id order by timestamp) gap from status_logs) t" },
  { "id": "avg-idle-per-station", "question": "At each workstation, what is the average time a component had been waiting since its own previous scan?",
    "expected_sql": "select w.name, avg(gap) from (select workstation_id, timestamp - lag(timestamp) over (partition by component_id order by timestamp) gap from status_logs) g join workstations w on w.id = g.workstation_id where gap is not null group by w.name" },
  { "id": "components-never-scanned", "question": "How many components have never been scanned?",
    "expected_sql": "select count(*) from components c where not exists (select 1 from status_logs s where s.component_id = c.id)" },
  { "id": "scans-per-component", "question": "How many scans does each component have?",
    "expected_sql": "select c.name, count(s.id) from components c left join status_logs s on s.component_id = c.id group by c.name" },

  { "id": "components-at-final-station", "question": "Which components are currently at the final station?",
    "expected_sql": "select c.name from components c join workstations w on w.id = c.current_workstation_id where w.is_final_station = true" },
  { "id": "components-at-qa-count", "question": "How many components are currently at a QA station?",
    "expected_sql": "select count(*) from components c join workstations w on w.id = c.current_workstation_id where w.is_qa = true" },
  { "id": "flagged-at-qa-count", "question": "How many flagged components are sitting at a QA station?",
    "expected_sql": "select count(*) from components c join workstations w on w.id = c.current_workstation_id where w.is_qa = true and c.current_status = 'flagged'" },
  { "id": "scans-at-final-station", "question": "How many scans happened at the final station?",
    "expected_sql": "select count(*) from status_logs s join workstations w on w.id = s.workstation_id where w.is_final_station = true" },
  { "id": "logs-missing-station-count", "question": "How many status log entries have no workstation recorded?",
    "expected_sql": "select count(*) from status_logs where workstation_id is null" },
  { "id": "components-without-station-count", "question": "How many components are not assigned to any workstation?",
    "expected_sql": "select count(*) from components where current_workstation_id is null" },

  { "id": "nonexistent-status-count", "question": "How many components have the status 'archived'?",
    "expected_sql": "select count(*) from components where current_status = 'archived'" },
  { "id": "nonexistent-station-count", "question": "How many workstations are called 'Warp Core'?",
    "expected_sql": "select count(*) from workstations where name = 'Warp Core'" },
  { "id": "duplicate-component-names", "question": "How many component names are used more than once?",
    "expected_sql": "select count(*) from (select name from components group by name having count(*) > 1) t" },
  { "id": "status-values-in-use", "question": "What distinct statuses are currently in use?",
    "expected_sql": "select distinct current_status from components" },
  { "id": "transitions-in-use", "question": "What status transitions have actually been recorded?",
    "expected_sql": "select distinct from_status, to_status from status_logs" }
]
